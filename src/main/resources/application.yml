server:
  port: 8000
spring:
  datasource:
    url: ${vcap.services.mysql.connection.jdbcUrl}
    username: ${cloud.services.mysql.connection.username}
    password: ${cloud.services.mysql.connection.password}
  jpa:
    hibernate:
      ddl-auto: create
cnj:
  info:
    message: Server verwendet Konfiguration aus Codebase!